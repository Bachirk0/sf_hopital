{% extends 'base.html.twig' %}
{% block contente %}
<table class="table">
    <thead>
        <tr>
            <th>id</th>
            <th>Libelle</th>
        </tr>
    </thead>
    <tbody>
     {% for service in services %}
        
   
        <tr>
            <td scope="row">{{service.id}}</td>
            <td>{{service.libelle}}
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
